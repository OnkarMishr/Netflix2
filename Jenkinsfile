pipeline {
    agent any
    
   
    
    stages {
        stage('Checkout') {
            steps {
                // Connect to Git repository using SCM
                checkout scm
            }
        }
        
        
    }
}
